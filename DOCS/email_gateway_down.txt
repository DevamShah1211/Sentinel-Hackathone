To:      sentinel.hackathon@gujarat.gov.in
Subject: RTSP returning 401 Unauthorized for credentials that worked earlier today — cam12/cam14, account devam6205@gmail.com

------------------------------------------------------------------------
BEFORE SENDING: add your team ID if you have one. No screenshots needed —
the evidence is the port/response table below, which is reproducible.

NOTE: this draft was rewritten on 11 September. The earlier version
described ports 8554/8889 refusing connections and the portal returning
502. That was the 8 September fault and it is NOT what is happening now:
today every port is open and the portal returns 200, and RTSP rejects a
credential that worked ninety minutes earlier. Do not send the old text.
------------------------------------------------------------------------

Dear Sentinel Hackathon Support Team,

We are reporting an authentication failure on the RTSP endpoint. This is
different from the connectivity problem we reported on 8 September: the
gateway is fully reachable today, and the failure is specifically a
rejected credential.

WHAT CHANGED, AND WHEN

This morning we ran our indexer successfully against the live grid:

    11:03 IST   cam12 and cam14 both opened, streams delivered frames
    11:11 IST   indexer stopped normally after an 8-minute run
    11:31 IST   all RTSP connections now return 401 Unauthorized

Nothing changed at our end between those times. Our stored credentials
have not been edited since 9 September, and the same username and
password were in use for the successful 11:03 run.

WHAT WE MEASURED, JUST NOW

Testing the gateway's TCP ports directly:

    Port 8554 (RTSP)     Open
    Port 8889 (WebRTC)   Open
    Port 443 (HTTPS)     Open
    Port 80  (HTTP)      Open

    https://cctv.corp8.cloud    HTTP 200

So the host is up and every service is listening. The failure is at the
authentication step, not the network:

    rtsp://103.250.160.189:8554/stream/cam14
    -> method DESCRIBE failed: 401 Unauthorized

    rtsp://103.250.160.189:8554/stream/cam12
    -> method DESCRIBE failed: 401 Unauthorized

A 401 on DESCRIBE means the connection succeeded and the server actively
rejected the credential. That is the opposite of the 8 September fault,
where the connection was refused before any credential was offered.

ONE FURTHER OBSERVATION

The HTTP camera-catalogue endpoint also returns an empty result for us
today, while the portal itself serves normally on 443. We mention it only
in case the two are related at your end; it is not our main question.

WHAT WOULD HELP

Simply knowing which of these is the case would be enough:

  1. Credentials were rotated as part of a scheduled change, and new ones
     will be issued — in which case we will wait for them.
  2. Our account was deactivated in error, and can be restored.
  3. This is expected during a migration and will resolve on its own.

Any of those answers is useful to us. We can retest within minutes of a
change at your end and confirm back to you.

We would be grateful for guidance today, as the submission window closes
on 15 September and the government-feed demonstration depends on this
access.

ACCOUNT

    Registered email : devam6205@gmail.com
    Tested cameras   : cam12, cam14
    Tested from      : our indexer and a direct RTSP client, same result
    Last success     : 11:03 IST, 11 September 2026

Kind regards,

Devam
KodeMatrix
devam6205@gmail.com
